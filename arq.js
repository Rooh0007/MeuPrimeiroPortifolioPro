var animado = document.getElementsByClassName('ring')
//animado.style.transform = `translate(${Math.random(10)},${Math.random(10)})`
console.log('rodolpho')
console.log(animado)
animado.map(()=>{
    
})
